package db

// migrate "github.com/mattes/migrate"

func (mdb *McflyDB) RunMigrate(direction string) {
	// var doMigrate func(string, string) ([]error, bool)
	// switch direction {
	// case "up":
	// 	// doMigrate = migrate.
	// case "down":
	// 	// doMigrate = migrate.DownSync
	// default:
	// 	log.Fatal(fmt.Errorf("No migrate direction %s . User up or down", direction))
	// }
}
